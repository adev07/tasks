// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    // fontFamily: 'Neutra Text, sans-serif', // Your desired font
  },
});

export default theme;
